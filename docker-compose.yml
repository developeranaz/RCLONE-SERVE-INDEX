version: '1'

services:
rclone:
    image: rclone/rclone
    restart: always
    env_file:
      - ./rclone.env
    ports:
      - "80:80/http"
      - "8000:8000/udp"
    privileged: true
    hostname: heroku-rclone-serve
    container_name: heroku-rclone-serve
